language: node_js
node_js: "12.12.0"
dist: xenial
services:
  - xvfb
addons:
  chrome: stable
branches:
      only:
      - master
before_script:
      - export DISPLAY=:99.0
      - npm install -g ionic cordova@^8.0.0
script:	
      - ionic cordova build --prod
